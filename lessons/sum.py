"""Summing the elements of a list using different loops."""
__author__ = "730578454"

idx: int = 0
list = []
def w_sum(vals):
    """Sum of values."""
    while idx < len(vals):
        w_sum = input()
        list.append(w_sum)
        total: sum[w_sum]
        print(total)
        idx += 1

idx_1: int = 0
list_1 = []
def f_sum(vals):
    """Sum of values not using range."""
    while idx_1 < len(vals):
        for elem in f_sum:
            f_sum = input()
            list_1.append(f_sum)
            total_2: sum[f_sum]
            print(total_2)
            idx_1 += 1

idx_2: int = 0
list_2 = []
def f_range_sum(vals):
    """Sum of values using range."""
    while idx_2 < len(vals):
        for idx_2 in range(0, len(f_range_sum)):
            elem: str = f_range_sum[idx]
            f_range_sum = input()
            list_2.append(f_range_sum)
            total_3: sum[f_range_sum]
            print(total_3)
            idx_2 += 1